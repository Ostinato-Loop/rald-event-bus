{
  "name": "@rald/event-bus",
  "version": "1.0.0",
  "private": true,
  "description": "RALD Event Bus — ecosystem event fabric. Every major action emits an event.",
  "scripts": {
    "dev": "wrangler dev --port 8790",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "biome check . --diagnostic-level=error"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10",
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.99.0"
  }
}
